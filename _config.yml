title: Policies
description: Official company policies and employee handbook
markdown: kramdown
remote_theme: just-the-docs/just-the-docs
color_scheme: brosnanOrange
#plugins:
#  - jekyll-seo-tag
enable_switcher: true

# Optional: Add company logo
logo: /assets/brosnan_logo.png
favicon_ico: /assets/brosnan_fav.png

Search_enabled: true
search:
  # Split pages into sections that can be searched individually
  # Supports 1 - 6, default: 2
  heading_level: 2
  # Maximum amount of previews per search result
  # Default: 3
  previews: 5
  # Maximum amount of words to display before a matched word in the preview
  # Default: 5
  preview_words_before: 5
  # Maximum amount of words to display after a matched word in the preview
  # Default: 10
  preview_words_after: 10
  # Set the search token separator
  # Default: /[\s\-/]+/
  # Example: enable support for hyphenated search words
  tokenizer_separator: /[\s/]+/
  # Display the relative url in search results
  # Supports true (default) or false
  rel_url: false
  # Enable or disable the search button that appears in the bottom right corner of every page
  # Supports true or false (default)
  button: true
  # Focus the search input by pressing `ctrl + focus_shortcut_key` (or `cmd + focus_shortcut_key` on macOS)
  focus_shortcut_key: 'k'

nav_enabled: true

heading_anchors: true

aux_links:
  "ELMO (MyToolKit)":
    - "https://brosnan.elmotalent.co.nz/dashboard"
# Makes Aux links open in a new tab. Default is false
aux_links_new_tab: true

back_to_top: true
back_to_top_text: "Back to top"

# Footer
footer_content: "<!-- footer is inserted via _includes/footer.html -->"
# This line tells Just-the-Docs to include your custom footer

mermaid:
  # Version of mermaid library
  # Pick an available version from https://cdn.jsdelivr.net/npm/mermaid/
  version: "11.12.1"

# Google Analytics Tracking (optional)
# Supports a CSV of tracking ID strings (eg. "UA-1234567-89,G-1AB234CDE5")
# Note: the main Just the Docs site does *not* use Google Analytics.
ga_tracking: G-9LCR4RMXSH
ga_tracking_anonymize_ip: false # Use GDPR compliant Google Analytics settings (true/nil by default)

callouts:
  lookatme:
    color: #F68B1F
    opacity: 0.3

# Exclude files
exclude:
  - .github/
  - README.md
  - LICENSE
